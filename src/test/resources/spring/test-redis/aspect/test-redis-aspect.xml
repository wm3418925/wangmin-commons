<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:p="http://www.springframework.org/schema/p"
       xmlns:aop="http://www.springframework.org/schema/aop"
       xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context.xsd
       http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop.xsd">
    <context:annotation-config />
    <aop:aspectj-autoproxy/>
    <context:component-scan base-package="wangmin.common.cache.annotation,com.test.redis" />

    <context:property-placeholder
		location="classpath:spring/test-redis/aspect/test-redis-aspect.properties" />

    <bean id="sunriseAspectJedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
        <property name="maxTotal" value="${sunrise.redis.aspect.count.redis.pool.maxTotal}"/>
        <property name="maxIdle" value="${sunrise.redis.aspect.count.redis.pool.maxIdle}"/>
        <property name="maxWaitMillis" value="${sunrise.redis.aspect.count.redis.pool.maxWaitMillis}"/>
        <property name="testOnBorrow" value="${sunrise.redis.aspect.count.redis.pool.testOnBorrow}"/>
        <property name="testOnReturn" value="${sunrise.redis.aspect.count.redis.pool.testOnReturn}"/>
    </bean>
    <bean id="wangminAnnotationCacheJedisConnectionFactory" class="org.springframework.data.redis.connection.jedis.JedisConnectionFactory"
          p:hostName="${sunrise.redis.aspect.count.redis.host}" p:port="${sunrise.redis.aspect.count.redis.port}" p:password="${sunrise.redis.aspect.count.redis.password}" p:database="${sunrise.redis.aspect.count.redis.db}" p:poolConfig-ref="sunriseAspectJedisPoolConfig"/>

    <bean id="kryoObjectSerializer" class="wangmin.common.cache.serializer.KryoObjectSerializer" />

    <bean name="wangminAnnotationCacheRepository" class="wangmin.common.cache.CacheRepositoryImpl">
        <property name="connFactory" ref="wangminAnnotationCacheJedisConnectionFactory" />
        <property name="defaultExpiration" value="3000" />
        <property name="objectSerializer" ref="kryoObjectSerializer" />
    </bean>

    <bean id="redisAnnotationCacheAspect" class="wangmin.common.cache.annotation.RedisAnnotationCacheAspect" />
</beans>
